import type { FraccionesBlok } from '../storyblok/types'

interface Props { blok: FraccionesBlok }

export default function FraccionesBlock({ blok: _blok }: Props) {
  // The interactive fraction viewer is currently in Home.tsx
  // Will be extracted when the Page renderer replaces Home.tsx
  return null
}
